package GUI;

public class Bobbypulido implements ActionListener {
    private Practicalistn frame;

    public Bobbypulido (Practicalistn frame)
    this.frame = frame;

    @Override
    public void actionPerformed(ActionEvent e) {
        for (int i = 0; i boton1.length; i++){
            if (e.getSource() == boton1){
contador1.setText(String.valeOF(contador1));
            }
        }

    }
}
